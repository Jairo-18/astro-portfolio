<style is:global>
  :root {
    /* Colores */
    --color-primary: #1d4ed8; /* Azul */
    --color-secondary: #9333ea; /* Violeta */
    --color-background: #ffffff; /* Gris claro */
    --color-text: #000000; /* Gris oscuro para el texto */
    --color-text-light: #fff; /* Blanco */

    /* Tamaños de fuente */
    --font-size-base: 16px;
    --font-size-sm: 14px;
    --font-size-md: 18px;
    --font-size-lg: 20px;
    --font-size-xl: 24px;

    /* Espaciados */
    --spacing-sm: 8px;
    --spacing-md: 12px;
    --spacing-lg: 16px;

    /* Tamaños de los contenedores */
    --container-max-width: 100%;
    --container-padding: 16px;

    --breakpoint-xs: 480px;
    --breakpoint-sm: 640px;
    --breakpoint-md: 768px;
    --breakpoint-lg: 1024px;
    --breakpoint-xl: 1280px;

    /* Tamaños de nav-bar */
    --navbar-height: 75px;
    --footer-height: auto;
  }
</style>
