<style is:global>
  :root {
    /* Colores */
    --color-primary: #1d4ed8; /* Azul */
    --color-secondary: #9333ea; /* Violeta */
    --color-background: #f3f4f6; /* Gris claro */
    --color-text: #333; /* Gris oscuro para el texto */
    --color-text-light: #fff; /* Blanco */

    /* Tamaños de fuente */
    --font-size-base: 16px;
    --font-size-lg: 18px;
    --font-size-xl: 24px;
    --font-size-xxl: 32px;

    /* Espaciados */
    --spacing-xs: 8px;
    --spacing-sm: 16px;
    --spacing-md: 24px;
    --spacing-lg: 32px;
    --spacing-xl: 48px;

    /* Tamaños de los contenedores */
    --container-max-width: 100%;
    --container-padding: 16px;

    --breakpoint-xs: 360px; /* Teléfonos pequeños */
    --breakpoint-sm: 480px; /* Teléfonos medianos */
    --breakpoint-md: 768px; /* Tablets */
    --breakpoint-lg: 1024px; /* Laptops */
    --breakpoint-xl: 1280px; /* Pantallas grandes */
  }
</style>
